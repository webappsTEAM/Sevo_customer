# Intentionally empty.
#
# This file held a temporary query-count diagnostic used to locate the N+1 in
# ServiceRequestListSerializer.get_payment_confirmation_otp. The sandbox that
# created it cannot delete files, so it is emptied rather than removed --
# safe to delete.
