# Generated by hand (not `makemigrations`) on 10 Sep 2026.
#
# `makemigrations orders` was not usable here: the autodetector inspects
# the whole project graph, and this merged codebase currently has
# unrelated, pre-existing pending schema drift in `service_requests`
# (e.g. an "AddOn.package" nullable->non-nullable change, index/constraint
# removals) that has nothing to do with the Order/OrderItem addition. Per
# this engagement's standing constraint -- never modify or generate
# migrations against ServiceRequest or other existing production models
# -- this migration is written by hand so it touches nothing outside the
# new `orders` app. The service_requests schema drift is a separate,
# pre-existing issue for that app's own owners to migrate deliberately.
#
# Depends on service_requests' latest migration at the time this was
# written (0075_merge_20260909_1634) only so Django can resolve the
# OneToOneField target -- this migration does not alter that app's schema
# in any way.
from django.conf import settings
from django.db import migrations, models
import django.db.models.deletion


class Migration(migrations.Migration):

    initial = True

    dependencies = [
        ("service_requests", "0075_merge_20260909_1634"),
        migrations.swappable_dependency(settings.AUTH_USER_MODEL),
    ]

    operations = [
        migrations.CreateModel(
            name="Order",
            fields=[
                ("id", models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name="ID")),
                ("order_number", models.CharField(blank=True, db_index=True, max_length=20, unique=True)),
                ("status", models.CharField(choices=[("draft", "Draft"), ("confirmed", "Confirmed"), ("cancelled", "Cancelled")], db_index=True, default="draft", max_length=12)),
                ("total_amount", models.DecimalField(decimal_places=2, default=0, max_digits=10)),
                ("created_at", models.DateTimeField(auto_now_add=True)),
                ("updated_at", models.DateTimeField(auto_now=True)),
                ("customer", models.ForeignKey(help_text="The customer who placed this order.", on_delete=django.db.models.deletion.PROTECT, related_name="orders", to=settings.AUTH_USER_MODEL)),
            ],
            options={
                "db_table": "orders_order",
                "ordering": ["-created_at"],
            },
        ),
        migrations.CreateModel(
            name="OrderItem",
            fields=[
                ("id", models.BigAutoField(auto_created=True, primary_key=True, serialize=False, verbose_name="ID")),
                ("service_category_snapshot", models.CharField(blank=True, default="", max_length=150)),
                ("item_amount", models.DecimalField(decimal_places=2, default=0, max_digits=10)),
                ("created_at", models.DateTimeField(auto_now_add=True)),
                ("updated_at", models.DateTimeField(auto_now=True)),
                ("order", models.ForeignKey(on_delete=django.db.models.deletion.CASCADE, related_name="items", to="orders.order")),
                ("service_request", models.OneToOneField(blank=True, null=True, on_delete=django.db.models.deletion.SET_NULL, related_name="order_item", to="service_requests.servicerequest")),
            ],
            options={
                "db_table": "orders_orderitem",
                "ordering": ["id"],
            },
        ),
    ]
